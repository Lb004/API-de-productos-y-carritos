<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{title}} - E-commerce</title>
    <style>
      * { margin: 0; padding: 0; box-sizing: border-box; }
      body { 
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); 
        min-height: 100vh; 
        padding: 20px; 
      }
      .container { 
        max-width: 1200px; 
        margin: 0 auto; 
        background: white; 
        border-radius: 15px; 
        padding: 30px; 
        box-shadow: 0 20px 60px rgba(0,0,0,0.3); 
      }
      h1 { 
        color: #333; 
        margin-bottom: 30px; 
        text-align: center; 
        font-size: 2.5rem; 
      }
      .nav-links { 
        display: flex; 
        gap: 15px; 
        justify-content: center; 
        margin-bottom: 30px; 
        flex-wrap: wrap; 
      }
      .nav-links a { 
        padding: 10px 20px; 
        background: #667eea; 
        color: white; 
        text-decoration: none; 
        border-radius: 5px; 
        transition: all 0.3s; 
      }
      .nav-links a:hover { 
        background: #764ba2; 
        transform: translateY(-2px); 
        box-shadow: 0 5px 15px rgba(0,0,0,0.2); 
      }
      .nav-links a.cart-link {
        background: #28a745;
      }
      .nav-links a.cart-link:hover {
        background: #218838;
      }
      .products-grid { 
        display: grid; 
        grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); 
        gap: 25px; 
        margin-top: 30px; 
      }
      .product-card { 
        background: #f8f9fa; 
        border-radius: 10px; 
        padding: 20px; 
        border: 2px solid #e9ecef; 
        transition: all 0.3s; 
        position: relative; 
      }
      .product-card:hover { 
        transform: translateY(-5px); 
        box-shadow: 0 10px 25px rgba(0,0,0,0.1); 
        border-color: #667eea; 
      }
      .product-card h3 { 
        color: #333; 
        margin-bottom: 10px; 
        font-size: 1.3rem; 
      }
      .product-card p { 
        color: #666; 
        margin: 8px 0; 
        line-height: 1.5; 
      }
      .product-price { 
        font-size: 1.5rem; 
        color: #667eea; 
        font-weight: bold; 
        margin: 15px 0; 
      }
      .product-code { 
        background: #667eea; 
        color: white; 
        padding: 5px 10px; 
        border-radius: 5px; 
        font-size: 0.85rem; 
        display: inline-block; 
        margin-top: 10px; 
      }
      .product-stock { 
        color: #28a745; 
        font-weight: bold; 
      }
      .product-stock.low { 
        color: #ffc107; 
      }
      .product-stock.out { 
        color: #dc3545; 
      }
      .no-products { 
        text-align: center; 
        padding: 60px 20px; 
        color: #666; 
        font-size: 1.2rem; 
      }
      .btn { 
        padding: 10px 20px; 
        border: none; 
        border-radius: 5px; 
        cursor: pointer; 
        font-size: 1rem; 
        transition: all 0.3s; 
        text-decoration: none; 
        display: inline-block; 
      }
      .btn-primary { 
        background: #667eea; 
        color: white; 
      }
      .btn-primary:hover { 
        background: #764ba2; 
      }
      .btn-danger { 
        background: #dc3545; 
        color: white; 
      }
      .btn-danger:hover { 
        background: #c82333; 
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>{{title}}</h1>

      <div class="nav-links">
        <a href="/">🏠 Inicio</a>
        <a href="/realtimeproducts">⚡ Tiempo Real</a>
        <a href="#" id="viewCartBtn" class="cart-link">🛒 Ver Mi Carrito</a>
      </div>

      {{{body}}}
    </div>

    <script>
      // Función para ver el carrito actual
      document.getElementById('viewCartBtn')?.addEventListener('click', function(e) {
        e.preventDefault();
        const cartId = localStorage.getItem('currentCartId');
        
        if (cartId) {
          window.location.href = `/carts/${cartId}`;
        } else {
          alert('⚠️ No tienes un carrito activo. Agrega productos primero.');
        }
      });
    </script>
  </body>
</html>